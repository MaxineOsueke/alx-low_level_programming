#include <stdio.h>

/**
 * main - causes an infinite loop
 * 0x - The number to be checked
 * Return: 1 for character that will be a digit or 0 for others
*/
int main(void)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
